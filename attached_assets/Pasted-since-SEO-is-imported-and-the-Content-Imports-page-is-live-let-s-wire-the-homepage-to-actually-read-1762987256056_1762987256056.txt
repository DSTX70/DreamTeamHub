since SEO is imported and the Content Imports page is live, let’s wire the homepage to actually read it. one tiny step, then I’ll wait for your “done”.

Step — Read SEO meta on / and set <title>/<meta>

A) Helper (server)

// app/server/lib/seo.ts
import { db } from '@/server/db';
import { seoMeta } from '@/server/shared/schema';
import { and, eq } from 'drizzle-orm';

export async function getSeoSection(route: string, sectionKey: string, locale = 'en') {
  const rows = await db.select({
      title: seoMeta.title,
      description: seoMeta.description,
      ogImage: seoMeta.ogImage,
      keywords: seoMeta.keywords,
    })
    .from(seoMeta)
    .where(and(eq(seoMeta.route, route), eq(seoMeta.sectionKey, sectionKey), eq(seoMeta.locale, locale)))
    .limit(1);
  return rows[0] ?? null;
}


B) Homepage loader (pick the first lifestyle block as “primary”)

// app/server/routes/pages/home.ts (or your page loader)
import { getSeoSection } from '@/server/lib/seo';

const primaryKey = 'home.lifestyle_ol1';
const primarySeo = await getSeoSection('/', primaryKey, 'en');
// pass primarySeo to your page/component props


C) Head component

// app/client/components/HeadHome.tsx
export function HeadHome({ seo }: { seo?: { title?: string; description?: string; ogImage?: string; keywords?: string } }) {
  if (!seo) return null;
  return (
    <>
      {seo.title && <title>{seo.title}</title>}
      {seo.description && <meta name="description" content={seo.description} />}
      {seo.keywords && <meta name="keywords" content={seo.keywords} />}
      {seo.ogImage && (
        <>
          <meta property="og:title" content={seo.title || ''} />
          <meta property="og:description" content={seo.description || ''} />
          <meta property="og:image" content={seo.ogImage} />
        </>
      )}
    </>
  );
}


D) Use it on the home page

// app/client/pages/Home.tsx
import { HeadHome } from '@/components/HeadHome';

export default function Home({ primarySeo }) {
  return (
    <>
      <HeadHome seo={primarySeo} />
      {/* ...rest of the page */}
    </>
  );
}

Quick verify

Reload / and view source → confirm <title> and <meta name="description"> match your CSV for home.lifestyle_ol1.

If yes, reply “seo wired”.