Below is a **single paste-ready patch drop** for the **DreamTeamHub repo** that implements both polish items:

1. **Clear context** button (removes context params + resets any prefilled filters where applicable)
2. **Standardized param names + banner copy** across **/ops/logs**, **/verification**, **/artifacts**

### Canonical query params (standardized)

* `workItemId`
* `context`
* `q`

### Standard banner (all 3 pages)

* Copy: **“Work item context detected”**
* Chips: **WorkItemId + Context**
* Actions: **Logs / Verification / Artifacts / Clear**

---

## ✅ PATCH DROP (DreamTeamHub)

```diff
FILE: client/src/lib/workItemContext.ts
+export const WORK_ITEM_ID_PARAM = "workItemId";
+export const CONTEXT_PARAM = "context";
+export const Q_PARAM = "q";
+
+export type WorkItemContext = {
+  workItemId?: string;
+  context?: string;
+  q?: string;
+};
+
+export function readWorkItemContext(sp: URLSearchParams): WorkItemContext {
+  const workItemId = sp.get(WORK_ITEM_ID_PARAM) || undefined;
+  const context = sp.get(CONTEXT_PARAM) || undefined;
+  const q = sp.get(Q_PARAM) || undefined;
+  return { workItemId, context, q };
+}
+
+export function hasWorkItemContext(ctx: WorkItemContext): boolean {
+  return Boolean(ctx.workItemId || ctx.context || ctx.q);
+}
+
+export function contextToSearch(ctx: WorkItemContext): string {
+  const sp = new URLSearchParams();
+  if (ctx.workItemId) sp.set(WORK_ITEM_ID_PARAM, ctx.workItemId);
+  if (ctx.context) sp.set(CONTEXT_PARAM, ctx.context);
+  if (ctx.q) sp.set(Q_PARAM, ctx.q);
+  const s = sp.toString();
+  return s ? `?${s}` : "";
+}
+
+export function clearWorkItemContextFromParams(sp: URLSearchParams): URLSearchParams {
+  const next = new URLSearchParams(sp);
+  next.delete(WORK_ITEM_ID_PARAM);
+  next.delete(CONTEXT_PARAM);
+  next.delete(Q_PARAM);
+  return next;
+}
+
END_FILE
```

```diff
FILE: client/src/pages/ops/ops-logs.tsx
@@
+import { Link, useLocation, useNavigate, useSearchParams } from "react-router-dom";
+import {
+  clearWorkItemContextFromParams,
+  contextToSearch,
+  hasWorkItemContext,
+  readWorkItemContext,
+} from "@/lib/workItemContext";
@@
 export default function OpsLogs() {
+  const navigate = useNavigate();
+  const location = useLocation();
+  const [searchParams, setSearchParams] = useSearchParams();
+  const workCtx = readWorkItemContext(searchParams);
+  const showCtx = hasWorkItemContext(workCtx);
+
+  const ctxSearch = contextToSearch(workCtx);
+
+  function handleClearContext() {
+    // 1) Clear URL params (workItemId/context/q)
+    const next = clearWorkItemContextFromParams(searchParams);
+    setSearchParams(next, { replace: true });
+
+    // 2) Also clear any prefilled filters so user doesn't feel "stuck"
+    // These setters/vars exist per your prior change-set:
+    try {
+      // @ts-expect-error - these are defined in this module; safe no-op if refactored later
+      setOwnerIdFilter?.("");
+      // @ts-expect-error
+      setOwnerTypeFilter?.("");
+      // @ts-expect-error - if you use a local q filter state
+      setQ?.("");
+    } catch {
+      // no-op
+    }
+
+    // 3) Keep them on the same page route
+    navigate(location.pathname, { replace: true });
+  }
@@
-  // existing code that reads params and shows "Prefilled from link..." note
+  // === Work-item context banner (standardized) ===
+  // Replace any older "Prefilled from link..." note with this unified banner.
@@
   return (
     <div className="space-y-4">
+      {showCtx && (
+        <div className="rounded-xl border p-3">
+          <div className="flex flex-col gap-2 md:flex-row md:items-center md:justify-between">
+            <div className="flex flex-col gap-2">
+              <div className="text-sm font-semibold">Work item context detected</div>
+              <div className="flex flex-wrap gap-2">
+                {workCtx.workItemId && (
+                  <span className="rounded-full border px-2 py-0.5 text-xs">
+                    WorkItemId: {workCtx.workItemId}
+                  </span>
+                )}
+                {workCtx.context && (
+                  <span className="rounded-full border px-2 py-0.5 text-xs">
+                    Context: {workCtx.context}
+                  </span>
+                )}
+              </div>
+            </div>
+
+            <div className="flex flex-wrap gap-2">
+              <Link
+                className="rounded-md border px-3 py-1 text-sm"
+                to={`/ops/logs${ctxSearch}`}
+              >
+                Logs
+              </Link>
+              <Link
+                className="rounded-md border px-3 py-1 text-sm"
+                to={`/verification${ctxSearch}`}
+              >
+                Verification
+              </Link>
+              <Link
+                className="rounded-md border px-3 py-1 text-sm"
+                to={`/artifacts${ctxSearch}`}
+              >
+                Artifacts
+              </Link>
+              <button
+                type="button"
+                className="rounded-md border px-3 py-1 text-sm"
+                onClick={handleClearContext}
+              >
+                Clear
+              </button>
+            </div>
+          </div>
+        </div>
+      )}
@@
       {/* rest of your existing logs UI */}
     </div>
   );
 }
 
 END_FILE
```

```diff
FILE: client/src/pages/verification.tsx
@@
+import { Link, useSearchParams } from "react-router-dom";
+import {
+  clearWorkItemContextFromParams,
+  contextToSearch,
+  hasWorkItemContext,
+  readWorkItemContext,
+} from "@/lib/workItemContext";
@@
 export default function Verification() {
+  const [searchParams, setSearchParams] = useSearchParams();
+  const workCtx = readWorkItemContext(searchParams);
+  const showCtx = hasWorkItemContext(workCtx);
+  const ctxSearch = contextToSearch(workCtx);
+
+  function handleClearContext() {
+    const next = clearWorkItemContextFromParams(searchParams);
+    setSearchParams(next, { replace: true });
+  }
@@
   return (
     <div className="space-y-4">
-      {/* existing "Work-item context detected" banner (older) */}
+      {showCtx && (
+        <div className="rounded-xl border p-3">
+          <div className="flex flex-col gap-2 md:flex-row md:items-center md:justify-between">
+            <div className="flex flex-col gap-2">
+              <div className="text-sm font-semibold">Work item context detected</div>
+              <div className="flex flex-wrap gap-2">
+                {workCtx.workItemId && (
+                  <span className="rounded-full border px-2 py-0.5 text-xs">
+                    WorkItemId: {workCtx.workItemId}
+                  </span>
+                )}
+                {workCtx.context && (
+                  <span className="rounded-full border px-2 py-0.5 text-xs">
+                    Context: {workCtx.context}
+                  </span>
+                )}
+              </div>
+            </div>
+            <div className="flex flex-wrap gap-2">
+              <Link className="rounded-md border px-3 py-1 text-sm" to={`/ops/logs${ctxSearch}`}>
+                Logs
+              </Link>
+              <Link className="rounded-md border px-3 py-1 text-sm" to={`/verification${ctxSearch}`}>
+                Verification
+              </Link>
+              <Link className="rounded-md border px-3 py-1 text-sm" to={`/artifacts${ctxSearch}`}>
+                Artifacts
+              </Link>
+              <button
+                type="button"
+                className="rounded-md border px-3 py-1 text-sm"
+                onClick={handleClearContext}
+              >
+                Clear
+              </button>
+            </div>
+          </div>
+        </div>
+      )}
 
       {/* Coverage page continues below */}
       {/* ...existing coverage UI... */}
     </div>
   );
 }
 
 END_FILE
```

```diff
FILE: client/src/pages/artifacts/projects.tsx
@@
+import { Link, useSearchParams } from "react-router-dom";
+import {
+  clearWorkItemContextFromParams,
+  contextToSearch,
+  hasWorkItemContext,
+  readWorkItemContext,
+} from "@/lib/workItemContext";
@@
 export default function Projects() {
+  const [searchParams, setSearchParams] = useSearchParams();
+  const workCtx = readWorkItemContext(searchParams);
+  const showCtx = hasWorkItemContext(workCtx);
+  const ctxSearch = contextToSearch(workCtx);
+
+  function handleClearContext() {
+    const next = clearWorkItemContextFromParams(searchParams);
+    setSearchParams(next, { replace: true });
+  }
@@
   return (
     <div className="space-y-4">
-      {/* existing context banner (older) */}
+      {showCtx && (
+        <div className="rounded-xl border p-3">
+          <div className="flex flex-col gap-2 md:flex-row md:items-center md:justify-between">
+            <div className="flex flex-col gap-2">
+              <div className="text-sm font-semibold">Work item context detected</div>
+              <div className="flex flex-wrap gap-2">
+                {workCtx.workItemId && (
+                  <span className="rounded-full border px-2 py-0.5 text-xs">
+                    WorkItemId: {workCtx.workItemId}
+                  </span>
+                )}
+                {workCtx.context && (
+                  <span className="rounded-full border px-2 py-0.5 text-xs">
+                    Context: {workCtx.context}
+                  </span>
+                )}
+              </div>
+            </div>
+            <div className="flex flex-wrap gap-2">
+              <Link className="rounded-md border px-3 py-1 text-sm" to={`/ops/logs${ctxSearch}`}>
+                Logs
+              </Link>
+              <Link className="rounded-md border px-3 py-1 text-sm" to={`/verification${ctxSearch}`}>
+                Verification
+              </Link>
+              <Link className="rounded-md border px-3 py-1 text-sm" to={`/artifacts${ctxSearch}`}>
+                Artifacts
+              </Link>
+              <button
+                type="button"
+                className="rounded-md border px-3 py-1 text-sm"
+                onClick={handleClearContext}
+              >
+                Clear
+              </button>
+            </div>
+          </div>
+        </div>
+      )}
 
       {/* Projects page continues below */}
       {/* ...existing projects UI... */}
     </div>
   );
 }
 
 END_FILE
```

---

## Notes (so you don’t get surprised)

* This patch **keeps your existing behavior** (prefill, banners) but makes it **consistent** and adds **Clear**.
* On **/ops/logs**, `Clear` attempts to reset prefilled filters too (via `setOwnerIdFilter`, `setOwnerTypeFilter`, `setQ`) **without breaking if you later refactor**.

---

## Post-apply verification (quick)

1. Open a Next Actions link with `?workItemId=…&context=…&q=…`
2. Confirm banner shows on all 3 pages with identical copy + actions
3. Click **Clear** on each page:

   * URL params removed
   * On logs: prefilled filter state is cleared (and the banner disappears)

If any of those three TS setter names in `ops-logs.tsx` differ (e.g., `setOwnerId` vs `setOwnerIdFilter`), tell me the exact names in that file and I’ll give you a micro-patch to align them.
