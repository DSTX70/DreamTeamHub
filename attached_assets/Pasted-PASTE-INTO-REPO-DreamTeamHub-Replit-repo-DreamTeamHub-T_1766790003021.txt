PASTE INTO REPO: **DreamTeamHub** (Replit repo: `DreamTeamHub`)

This is **out of scope for the DriveSteward root thread** (it’s a DreamTeamHub client page change). Apply this patch in **DreamTeamHub** to swap the local shadowing `WorkItem` interface to the canonical type from `@shared/schema`.

```text
FILE: client/src/pages/ops/AltTextImport.tsx
import { useState } from 'react';
import { useQuery, useMutation } from '@tanstack/react-query';
import type { WorkItem } from '@shared/schema';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Alert, AlertDescription } from '@/components/ui/alert';
import { Badge } from '@/components/ui/badge';
import { FileText, Upload, CheckCircle, AlertCircle, Download } from 'lucide-react';
import { apiRequest, queryClient } from '@/lib/queryClient';

interface WorkItemFile {
  id: number;
  workItemId: number;
  filename: string;
  s3Key: string;
  s3Url: string;
  uploadedAt: string;
}
END_FILE
```

What this does:

* ✅ Adds `import type { WorkItem } from '@shared/schema'`
* ✅ Removes the local `interface WorkItem { ... }` shadow type
* ✅ Leaves `WorkItemFile` intact (since you only flagged WorkItem as safe to swap)

---

## Canonical thread placement + starter copy

Create/continue work in a **DreamTeamHub execution thread** for any further ops UI/schema alignment.

Paste this to start a new thread:

```text
DreamTeamHub — Ops UI Type Alignment (Canonical)
Confidential and proprietary and copyright Dustin Sparks 2025

PASTE TARGET
- Repo/Replit: DreamTeamHub

CHANGE
- Replace shadowing local WorkItem interfaces with canonical types from @shared/schema where safe.

FILE(S)
- client/src/pages/ops/AltTextImport.tsx (WorkItem swap done)

NEXT
- Identify any other pages with shadowed types (WorkOrder, DropInbox, PublishHistory rows, etc.)
- Provide patch drops per file (single FILE:…END_FILE blocks)

DEFINITION OF DONE
- No shadowed canonical types where schema import is safe
- Builds clean (tsc) and pages compile
```

If you paste the rest of `AltTextImport.tsx` (or the error output if it fails to compile), I’ll adjust the patch to match exact usage (e.g., generics in `useQuery<WorkItem[]>`, etc.).
